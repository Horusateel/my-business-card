import React from "react";

const About = () => {
    return (
        <section className="about">
            <h3>About</h3>
            <p>Great number of skills, each to a high dgree of quality. Those include proficiency in each of:
                JavaScript, Python, C and SQLite. And currently practicing both Node.JS and React.JS concurrently.
            </p>
        </section>
    );
}


export default About;